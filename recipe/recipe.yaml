schema_version: 1

context:
  name: displayhooks
  version: "0.0.6"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/displayhooks-${{ version }}.tar.gz
  sha256: 38e77bc26fc425dd10a949017b23aebc0629d60d7b1c309bc160619cf7afa775

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools ==68.0.0
    - pip
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - displayhooks
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Extend the capabilities of the standard displayhook in Python
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/pomponchik/displayhooks

extra:
  recipe-maintainers:
    - thewchan
